# Croppa v2 - Production Configuration
# Copy this to .env and fill in your values:  cp .env.example .env

# Your domain (Caddy auto-provisions TLS certs via Let's Encrypt)
DOMAIN=croppa.example.com

# Basic auth credentials
AUTH_USER=croppa
# Generate the hash with:
#   docker run --rm caddy:2-alpine caddy hash-password --plaintext 'your-password-here'
# Then paste the full output (starts with $2a$) below:
AUTH_PASS_HASH=
